"use client";
import { useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { FriendButton } from "./FriendButton";
import { PostCard } from "../PostCard";
import { PostEngagement } from "./PostEngagement";
import { PostContent } from "./PostContent";
import { Avatar } from "./Avatar";
import { Icon } from "./Icon";
import { Modal } from "./Modal";
import { postExcerpt, readDocument, imageSources } from "@/lib/post-content";
import type { blogData } from "@/lib/blog-data";
type Data = NonNullable<Awaited<ReturnType<typeof blogData>>>;
export function BlogExplorer({ data, initialPostId, initialCategory = "all", embedded = false }: { data: Data; initialPostId?: string; initialCategory?: string; embedded?: boolean }) {
  const router = useRouter(); const category = initialCategory; const [postId, setPostId] = useState(initialPostId || null); const [deleteOpen, setDeleteOpen] = useState(false); const [deleting, setDeleting] = useState(false);
  const post = data.posts.find((item) => item.id === postId); const filtered = data.posts; const totalPages = Math.max(1, Math.ceil(data.totalPosts / data.pageSize));
  const blogUrl = "/blog/" + data.author.id; const listUrl = embedded ? "/mypage?tab=posts&category=" + encodeURIComponent(category) : blogUrl + "?category=" + encodeURIComponent(category);
  async function deletePost() { if (!post || deleting) return; setDeleting(true); await fetch("/api/posts/" + post.id, { method: "DELETE" }); setDeleteOpen(false); setPostId(null); router.refresh(); setDeleting(false); }
  const categoryCount = (id: string) => data.categoryCounts.find((item) => (item.categoryId || "uncategorized") === id)?.count || 0;
  return <div className="blog-explorer"><aside className="blog-sidebar"><Link href={blogUrl} className="blog-owner"><Avatar src={data.author.image} name={data.author.name} size={64} /><strong>{data.author.name || "개발자"}</strong>{data.author.bio && <p className="blog-owner-bio">{data.author.bio}</p>}<small>{data.author.nickname ? data.author.nickname + "#" + data.author.tag : "Developer Blog"}</small></Link><FriendButton authorId={data.author.id} viewerId={data.viewerId} friendship={data.friendship} /><nav aria-label="작성자 카테고리"><span className="blog-sidebar-label">CATEGORIES</span><Link href={listUrl} className="blog-category"><span>전체 글</span><small>{data.totalPosts}</small></Link>{data.categories.map((item) => item.isDivider ? <hr key={item.id} /> : <Link key={item.id} href={(embedded ? "/mypage?tab=posts&category=" : blogUrl + "?category=") + encodeURIComponent(item.id) + "#post-list"} className="blog-category"><span>{item.name}</span><small>{categoryCount(item.id)}</small></Link>)}<Link href={(embedded ? "/mypage?tab=posts&category=uncategorized" : blogUrl + "?category=uncategorized") + "#post-list"} className="blog-category"><span>미분류</span><small>{categoryCount("uncategorized")}</small></Link></nav>{data.isOwner && <Link href="/write" className="button button-accent w-full mt-6"><Icon name="pen" width={16} height={16} />글 작성</Link>}</aside><section className="blog-reading">{post ? <article><div className="blog-reading-toolbar"><Link href={listUrl + "#post-list"} className="nav-link">목록으로</Link>{data.isOwner && <button className="button button-secondary" onClick={() => setDeleteOpen(true)}>삭제</button>}</div><h1 className="blog-post-title">{post.title}</h1><div className="blog-post-date"><Link href={blogUrl}><Avatar src={data.author.image} name={data.author.name} size={32} /><strong>{data.author.name || "개발자"}</strong></Link><time>{new Date(post.createdAt).toLocaleDateString("ko-KR")}</time></div><PostContent content={post.content} idPrefix={"post-" + post.id} /><PostEngagement key={post.id} postId={post.id} /></article> : <><div className="section-heading"><h2 id="post-list">{category === "all" ? "전체 글" : "글 목록"}</h2><span>{data.totalPosts}개의 글</span></div>{filtered.length ? <div className="home-post-grid">{filtered.map((item) => { const doc = readDocument(item.content); return <PostCard key={item.id} currentUserId={data.viewerId} views={item._count.views} href={(embedded ? listUrl + "&post=" + item.id : "/posts/" + item.id) + "#post-start"} post={{ ...item, authorId: data.author.id, author: data.author, category: data.categories.find((cat) => cat.id === item.categoryId) || null, excerpt: postExcerpt(item.content), thumbnail: doc ? imageSources(doc)[0] || null : null, likes: item._count.likes, liked: item.likes.length > 0 }} />; })}</div> : <div className="empty-state"><h3>아직 글이 없습니다</h3></div>}{totalPages > 1 && <nav className="feed-pagination"><Link href={data.page > 1 ? listUrl + "&page=" + (data.page - 1) + "#post-list" : "#post-list"}>이전</Link><span>{data.page} / {totalPages}</span><Link href={data.page < totalPages ? listUrl + "&page=" + (data.page + 1) + "#post-list" : "#post-list"}>다음</Link></nav>}</>}</section><Modal open={deleteOpen} onClose={() => setDeleteOpen(false)} title="글 삭제"><div className="modal-form"><p>이 글을 삭제할까요?</p><div className="modal-actions"><button className="button button-secondary" onClick={() => setDeleteOpen(false)}>취소</button><button className="button button-primary" disabled={deleting} onClick={() => void deletePost()}>삭제</button></div></div></Modal></div>;
}
